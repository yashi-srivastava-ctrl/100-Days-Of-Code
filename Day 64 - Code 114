Q114: Write a program to take a string s as input. The task is to find the length of the longest substring without repeating characters. Print the length as output.

/*
Sample Test Cases:
Input 1:
s = "abcabcbb"
Output 1:
3

Input 2:
s = "bbbbb"
Output 2:
1

Input 3:
s = "pwwkew"
Output 3:
3

*/

#include <stdio.h>
#include <string.h>

#define CHAR_RANGE 256  // Number of possible ASCII characters

int lengthOfLongestSubstring(char *s) {
    int n = strlen(s);
    int lastIndex[CHAR_RANGE];
    for (int i = 0; i < CHAR_RANGE; i++) {
        lastIndex[i] = -1;  // Initialize all indices as -1
    }

    int maxLen = 0;
    int start = 0;  // Left boundary of the sliding window

    for (int i = 0; i < n; i++) {
        char ch = s[i];
        if (lastIndex[(int)ch] >= start) {
            start = lastIndex[(int)ch] + 1;  // Move start right after previous occurrence
        }
        lastIndex[(int)ch] = i;
        int currLen = i - start + 1;
        if (currLen > maxLen) {
            maxLen = currLen;
        }
    }

    return maxLen;
}

int main() {
    char s[1000];
    fgets(s, sizeof(s), stdin);
    s[strcspn(s, "\n")] = '\0';  // Remove newline if present

    int result = lengthOfLongestSubstring(s);
    printf("%d\n", result);

    return 0;
}
