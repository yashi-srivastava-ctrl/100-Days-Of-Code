Q41: Write a program to swap the first and last digit of a number.

/*
Sample Test Cases:
Input 1:
1234
Output 1:
4231

Input 2:
1001
Output 2:
1001

*/

#include <stdio.h>
#include <math.h>

int main() {
    int num, firstDigit, lastDigit, numDigits, swappedNum, power;

    printf("Enter a number: ");
    scanf("%d", &num);

    if (num < 10) {
        // For single-digit numbers, swapping doesn't change anything
        printf("Number after swapping first and last digits: %d\n", num);
        return 0;
    }

    lastDigit = num % 10;

    numDigits = (int)log10(num);  // Number of digits - 1
    power = (int)pow(10, numDigits);

    firstDigit = num / power;

    // Remove first digit
    swappedNum = num % power;

    // Remove last digit from swappedNum and add firstDigit at the end
    swappedNum = swappedNum / 10 * 10 + firstDigit;

    // Add lastDigit at the front
    swappedNum = swappedNum + lastDigit * power;

    printf("Number after swapping first and last digits: %d\n", swappedNum);

    return 0;
}
