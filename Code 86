Q86: Check if a string is a palindrome.

/*
Sample Test Cases:
Input 1:
madam
Output 1:
Palindrome

Input 2:
hello
Output 2:
Not palindrome

*/

#include <stdio.h>

int main() {
    char str[1000];
    fgets(str, sizeof(str), stdin);

    // Remove newline if present
    int length = 0;
    while (str[length] != '\0' && str[length] != '\n') {
        length++;
    }

    int start = 0, end = length - 1;
    int palindrome = 1;  // Assume palindrome until disproved

    while (start < end) {
        if (str[start] != str[end]) {
            palindrome = 0;
            break;
        }
        start++;
        end--;
    }

    if (palindrome) {
        printf("Palindrome\n");
    } else {
        printf("Not palindrome\n");
    }

    return 0;
}
